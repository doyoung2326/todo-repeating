{
  "name": "study-todo-app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently -n 서버,화면 -c cyan,magenta \"node backend/server.js\" \"cd frontend && npm run dev\"",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "node backend/server.js",
    "start:frontend": "cd frontend && npm run dev",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
