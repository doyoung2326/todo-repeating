# 배포 전 체크리스트

개발 중에는 비워 두고 **스토어에 올리기 직전에** 채우는 것들.
코드에 플레이스홀더로 박혀 있어서, 안 채우면 심사에서 걸리거나 앱이 잘못된 곳을 바라본다.

문서로 남기는 이유 — 이 값들은 코드를 읽어서는 "비어 있다"는 것을 알기 어렵다.
`APP_STORE_CONNECT_앱_ID를_여기에` 같은 문자열은 문법적으로 멀쩡해서 빌드도 테스트도 통과한다.

---

## 1. 개인정보 보호책임자 — 이름·연락처

**파일:** `frontend/public/privacy.html` (10절 표)

```
[담당자명]      → 실제 이름
[연락 이메일]   → 문의를 받을 주소
```

개인정보 보호법 제30조가 요구하는 항목이라 비워 두면 방침으로서 요건을 못 채운다.
파일 맨 위 주석에도 같은 표시를 해 뒀다.

> 공개 문서라 개인 이메일을 임의로 넣지 않았다. 실제로 받을 주소를 정해서 넣을 것.

## 2. 개인정보처리방침 주소

**파일:** `mobile/src/constants/links.ts`

```
PRIVACY_URL = 'https://todo-repeating.vercel.app/privacy'
```

**확인 완료(2026-08-19).** `/`·`/privacy`·`/privacy.html` 모두 200이고 내용도 이 앱의 것이다.

- 처음에는 `study-todo.vercel.app`이 박혀 있었다. **그것은 남이 선점한 주소**여서
  방침 링크가 404였다 — 그대로 뒀으면 심사에서 반려됐을 자리다.
  Vercel 도메인은 저장소 이름(`todo-repeating`)을 따라간다.
- 도메인을 바꿀 일이 있으면 **브라우저로 열어 200이 오는지** 다시 확인한다.
  이 주소가 앱스토어·구글플레이 심사와 AdSense에 제출할 URL이다.
- `/privacy`는 `vercel.json`의 rewrite가 `/privacy.html`로 보내주는 것이다. rewrite를 지우면
  앱 쪽 링크가 404가 된다.
- 웹(`frontend/src/config/links.js`)은 `/privacy.html`을 그대로 쓴다. **바꾸지 말 것** —
  설치한 PWA의 서비스 워커가 프리캐시해 둔 키가 그 주소라, `/privacy`로 바꾸면
  오프라인에서 방침 대신 앱 껍데기가 뜬다.

## 3. App Store Connect 앱 ID

**파일:** `mobile/eas.json` → `submit.production.ios.ascAppId`

```
"ascAppId": "APP_STORE_CONNECT_앱_ID를_여기에"
```

App Store Connect에서 앱을 만든 뒤 발급되는 숫자 ID로 바꾼다.
`eas submit --platform ios`가 이 값을 쓴다.

## 4. 서버 주소 확인 (`EXPO_PUBLIC_API_URL`)

**파일:** `mobile/eas.json`의 `build.preview` / `build.production`

지금은 둘 다 `https://todo-repeating-production.up.railway.app/api`다.
운영 서버 주소가 바뀌었으면 여기도 바꾼다 — **빌드 시점에 앱에 박히므로**, 잘못 박히면
앱을 다시 심사받아야 고쳐진다. (`shared/apiUrl.js`가 값이 없거나 상대 경로면 빌드를 실패시킨다.)

---

## 손으로 눌러봐야 하는 것

자동 테스트가 없는 구간이다. 앱은 아직 실기기에서 확인하지 않았다.

### 앱 (`npm run start:mobile`)

1. 설정 탭 → **회원 탈퇴** → 비밀번호를 **틀리게** 입력
   → 오류가 창 안에 남고 **로그아웃되지 않아야 한다.** (서버가 403을 주는 이유가 이것이다)
2. 비밀번호를 맞게 입력 → 로그인 화면으로 돌아온다.
3. 같은 이메일로 **다시 가입**된다. 가입 후 할 일 목록이 비어 있다.
4. 로그인 화면 아래 **개인정보처리방침** → 앱 위에 브라우저가 뜬다(앱을 떠나지 않는다).
5. 안드로이드에서 탈퇴 창을 **뒤로가기**로 닫아본다.

### 웹 (`npm start`)

1. 넓은 화면(≥700px) 헤더의 **회원 탈퇴**, 좁은 화면(<700px) 계정 메뉴의 **회원 탈퇴** 둘 다.
2. 비밀번호 오입력 시 로그아웃되지 않는지 — 앱과 같은 확인.
3. 설치한 PWA에서 **비행기 모드로** `/privacy.html`이 열리는지(프리캐시 확인).

---

## 알려진 것 (고칠지 정해야 함)

- **`PUT /api/auth/password`가 비밀번호 오입력에 401을 준다** (`backend/app.js`).
  `shared/authFetch.js`가 401을 세션 만료로 보고 로그아웃시키므로, 현재 비밀번호를 한 번
  잘못 치면 조용히 로그아웃된다. 회원 탈퇴 라우트는 같은 함정을 피하려고 403을 쓴다.
  → 403으로 바꾸면 해결된다. 별건이라 이번에는 손대지 않았다.
